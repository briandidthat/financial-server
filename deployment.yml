apiVersion: apps/v1
kind: Deployment
metadata:
  name: price-server
  labels:
    app: price-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: price-server
  template:
    metadata:
      labels:
        app: price-server
    spec:
      containers:
        - name: price-server
          image: price-server:1.0.1
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
              httpHeaders:
                - name: caller
                  value: kubernetes
            periodSeconds: 180
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: price-server-service
spec:
  type: NodePort
  selector:
    app: price-server
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
